
// router.get('/comments', authenticate, getCommentById);

// router.post('/comments', authenticate, validationMiddleware(createComment), );

// router.patch('/rooms/:roomId', authenticate, updateCommentById);

// router.delete('/rooms/:roomId', authenticate, deleteCommentByID);
//I should implement this for only the commenter with the postID